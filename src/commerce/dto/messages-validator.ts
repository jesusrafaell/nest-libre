const textNotEmpty = {
  message: '$property no puede ser vacio',
};

const textLength = {
  message: '$property longitud invalida',
};

const afiliadoLength = {
  message: '$property debe ser igual a 15 caracteres',
};

const codigoBanco = {
  message: '$property debe ser igual a 4 caracteres',
};

const cuentaBanco = {
  message: '$property debe ser igual a 20 caractere',
};

const isBoolean = {
  message: '$property debe ser un booleano',
};

const isDefined = {
  message: '$property no puede estar vacio',
};

const isObject = {
  message: '$property debe ser un objeto',
};

export default {
  textNotEmpty,
  textLength,
  codigoBanco,
  cuentaBanco,
  afiliadoLength,
  isBoolean,
  isDefined,
  isObject,
};
